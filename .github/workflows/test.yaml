name: Action Sample

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    branches: [main]

jobs:
  numpy-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'
          architecture: 'x64'
      - name: Get Python version
        run: python -V
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run Python
        run: 
            python post_message_test.py
        env:
            APP_LEVEL_TOKEN:${{ secrets.APP_LEVEL_TOKEN }}
            BOT_USER_OAUTH_TOKEN:${{ secrets.BOT_USER_OAUTH_TOKEN }}

